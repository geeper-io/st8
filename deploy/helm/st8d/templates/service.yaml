apiVersion: v1
kind: Service
metadata:
  name: {{ include "st8d.fullname" . }}
  labels:
    {{- include "st8d.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "st8d.selectorLabels" . | nindent 4 }}
